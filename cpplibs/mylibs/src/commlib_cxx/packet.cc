#include "packet.h"

namespace commlib
{
	NetPacket* NetPacket::TakePacket(size_t size)
	{
		NetPacket* pkt = nullptr;

		return pkt;
	}

	void NetPacket::ReleasePakcet(NetPacket* pkt)
	{
		printf("not implemented yet!!!");

	}
}
